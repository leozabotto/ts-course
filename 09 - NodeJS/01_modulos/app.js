/* CÓDIGO NÃO EXECUTÁVEL */

const mod1 = require('./mod1')
console.log(mod1)

//const { nome, sobrenome, falaNome } = require('./mod1)
//const falaNome = require('./mod1').falaNome

// Módulos do Node devem ser importados sem o .
const path = require('path')

// Exportando
module.exports {
  exp1, exp2, exp3 [...]
}